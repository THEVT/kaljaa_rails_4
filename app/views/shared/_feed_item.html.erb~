<li id="<%= feed_item.id %>">
	<%= link_to feed_item.account %>
	<span class="user">
		<%= link_to feed_item.account.email, feed_item.account %>
	</span>
	<span class="content"><%= feed_item.content %></span>
	<span class="timestamp">
		Posted <%= time_ago_in_words(feed_item.created_at) %> ago.
	</span>
</li>
