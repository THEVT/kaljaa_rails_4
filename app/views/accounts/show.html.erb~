<h1>hello world</h1>

<section>
	<%= render 'shared/stats' %>
</section>

<% if @account.microposts.any? %>
	<h3>Microposts (<%= @account.microposts.count %>)</h3>
	<ol class="microposts">
		<%= render @microposts %>
	</ol>
	<%= will_paginate @microposts %>
<% end %>
